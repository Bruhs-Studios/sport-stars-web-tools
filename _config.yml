title: SportStars Tooling Hub
description: Internal game design & economy balancing tools.

# Disabled default theme to use custom Tailwind layout
# theme: jekyll-theme-slate 

defaults:
  - scope:
      path: "tools"
    values:
      layout: null
